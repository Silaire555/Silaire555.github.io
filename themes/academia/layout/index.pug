extends includes/layout

block content
    .page(itemscope itemtype='http://schema.org/CreativeWork')
        each article in page.posts.data
            if article.academia
                h2.post-title
                    a(href=url_for(article.path))= article.title
                if article.excerpt
                    != article.excerpt
                    p: a(href=url_for(article.path)) 阅读更多
                else
                    != article.content